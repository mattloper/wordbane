# --- Godot ---
# Editor import cache & generated metadata (regenerated on first open/run).
game/.godot/
*.import
export_presets.cfg

# --- Python / uv ---
.venv/
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/

# --- OS ---
.DS_Store

# NOTE: game/data/word_bank.json IS committed on purpose — it's generated by the
# Python tooling but the game depends on it at runtime. Regenerate with:
#   cd tools && uv run wordplay-generate
